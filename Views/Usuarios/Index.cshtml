@model IEnumerable<Tienda_Repuestos_Demo.Models.Usuario>

@{
    ViewData["Title"] = "Gestión de Usuarios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4 flex-column flex-md-row">
        <div class="mb-3 mb-md-0 flex-grow-1">
            <h2 class="mb-1" style="color: #ffffff; font-weight: 700;">👥 Gestión de Usuarios</h2>
            <p class="text-muted mb-0">Administra los usuarios del sistema (admin y vendedor)</p>
        </div>
        <a asp-action="Create" class="btn btn-primary w-100 w-md-auto flex-shrink-0">
            ➕ Nuevo Usuario
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <!-- Versión Desktop: Tabla -->
            <div class="d-none d-md-block">
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Correo</th>
                                <th>Rol</th>
                                <th>Estado</th>
                                <th>Fecha Registro</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Nombre</td>
                                    <td>@item.Correo</td>
                                    <td>
                                        @if (item.Rol == "admin")
                                        {
                                            <span class="badge bg-danger">👨‍💼 Admin</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-info">👤 Vendedor</span>
                                        }
                                    </td>
                                    <td>
                                        @if (item.Activo)
                                        {
                                            <span class="badge bg-success">✓ Activo</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">✗ Inactivo</span>
                                        }
                                    </td>
                                    <td>@item.FechaRegistro.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="Details" asp-route-id="@item.IdUsuario" class="btn btn-sm btn-info">👁️ Ver</a>
                                            <a asp-action="Edit" asp-route-id="@item.IdUsuario" class="btn btn-sm btn-warning">✏️ Editar</a>
                                            <a asp-action="Delete" asp-route-id="@item.IdUsuario" class="btn btn-sm btn-danger">🗑️ Eliminar</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Versión Móvil: Cards -->
            <div class="d-block d-md-none">
                @foreach (var item in Model)
                {
                    <div class="card mb-3" style="border-left: 4px solid @(item.Rol == "admin" ? "#dc3545" : "#17a2b8");">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-start mb-2">
                                <div>
                                    <h6 class="mb-1" style="color: #212529; font-weight: 700;">@item.Nombre</h6>
                                    <small class="text-muted">📧 @item.Correo</small>
                                </div>
                                <div class="text-end">
                                    @if (item.Rol == "admin")
                                    {
                                        <span class="badge bg-danger">👨‍💼 Admin</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-info">👤 Vendedor</span>
                                    }
                                    <br>
                                    @if (item.Activo)
                                    {
                                        <span class="badge bg-success mt-1">✓ Activo</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary mt-1">✗ Inactivo</span>
                                    }
                                </div>
                            </div>
                            
                            <div class="mb-3">
                                <strong style="color: #495057;">📅 Registro:</strong>
                                <span style="color: #212529;">@item.FechaRegistro.ToString("dd/MM/yyyy")</span>
                            </div>
                            
                            <div class="d-grid gap-2">
                                <a asp-action="Details" asp-route-id="@item.IdUsuario" class="btn btn-sm btn-info">👁️ Ver Detalles</a>
                                <a asp-action="Edit" asp-route-id="@item.IdUsuario" class="btn btn-sm btn-warning">✏️ Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.IdUsuario" class="btn btn-sm btn-danger">🗑️ Eliminar</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
