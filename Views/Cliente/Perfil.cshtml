@model Tienda_Repuestos_Demo.Models.Cliente

@{
    ViewData["Title"] = "Mi Perfil";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1" style="color: var(--text-dark); font-weight: 700;">👤 Mi Perfil</h2>
            <p class="text-muted mb-0">Actualiza tu información personal</p>
        </div>
    </div>

    @if (ViewBag.Success != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <strong>✅ Éxito:</strong> @ViewBag.Success
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">📝 Editar Información</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Perfil" method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="IdCliente" />
                        <input type="hidden" asp-for="FechaRegistro" />
                        
                        <div class="mb-3">
                            <label asp-for="Nombre" class="form-label">👤 Nombre Completo</label>
                            <input asp-for="Nombre" class="form-control" required />
                            <span asp-validation-for="Nombre" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Correo" class="form-label">📧 Correo Electrónico</label>
                            <input asp-for="Correo" type="email" class="form-control" required />
                            <span asp-validation-for="Correo" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Telefono" class="form-label">📞 Teléfono</label>
                            <input asp-for="Telefono" class="form-control" />
                            <span asp-validation-for="Telefono" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Direccion" class="form-label">📍 Dirección</label>
                            <textarea asp-for="Direccion" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Direccion" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label for="fotoCI" class="form-label">🆔 Foto de Cédula de Identidad</label>
                            @if (!string.IsNullOrEmpty(Model.FotoCI))
                            {
                                <div class="mb-2">
                                    <img src="@Model.FotoCI" alt="Foto CI" class="img-thumbnail" style="max-width: 200px; max-height: 200px;" />
                                    <p class="text-muted small mb-0">Foto actual</p>
                                </div>
                            }
                            <input type="file" class="form-control" id="fotoCI" name="fotoCI" accept="image/*" />
                            <small class="text-muted">
                                Formatos permitidos: JPG, PNG, GIF. Tamaño máximo: 5MB. Dejar vacío para mantener la foto actual.<br>
                                <strong class="text-success">✅ Si subes un CI nuevo, tu cuenta será verificada automáticamente</strong>
                            </small>
                        </div>

                        <div class="mb-3">
                            <div class="alert alert-info">
                                <strong>📋 Estado de Verificación:</strong>
                                @if (Model.Verificado)
                                {
                                    <span class="badge bg-success ms-2">✓ Verificado</span>
                                    <p class="mb-0 mt-2 small">Tu cuenta está verificada. Puedes realizar compras sin restricciones.</p>
                                }
                                else
                                {
                                    <span class="badge bg-warning ms-2">Pendiente</span>
                                    <p class="mb-0 mt-2 small">Sube una foto de tu CI para verificar tu cuenta automáticamente.</p>
                                }
                            </div>
                        </div>

                        <div class="mb-3">
                            <button type="submit" class="btn btn-primary">💾 Guardar Cambios</button>
                            <a asp-action="Dashboard" class="btn btn-secondary">↩️ Volver</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">ℹ️ Información</h5>
                </div>
                <div class="card-body">
                    <p class="text-muted">
                        <strong>📅 Fecha de Registro:</strong><br>
                        @Model.FechaRegistro.ToString("dd/MM/yyyy HH:mm")
                    </p>
                    <hr>
                    <p class="text-muted small">
                        <strong>Nota:</strong> Para cambiar tu contraseña, contacta con el administrador.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
