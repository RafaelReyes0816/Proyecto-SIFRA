@model Tienda_Repuestos_Demo.Models.Producto

@{
    ViewData["Title"] = "Detalles del Producto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1" style="color: var(--text-dark); font-weight: 700;">📦 Detalles del Producto</h2>
            <p class="text-muted mb-0">Información completa del producto</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">📋 Información General</h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4" style="color: var(--text-medium);">🏷️ Código:</dt>
                        <dd class="col-sm-8"><strong>@(Model.Codigo ?? "N/A")</strong></dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">📦 Nombre:</dt>
                        <dd class="col-sm-8"><strong>@Model.Nombre</strong></dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">📝 Descripción:</dt>
                        <dd class="col-sm-8">@(Model.Descripcion ?? "Sin descripción")</dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">📂 Categoría:</dt>
                        <dd class="col-sm-8"><span class="badge bg-info">@Model.Categoria?.Nombre</span></dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">🏢 Proveedor:</dt>
                        <dd class="col-sm-8">@Model.Proveedor?.Nombre</dd>
                    </dl>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">💰 Precios y Stock</h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-6" style="color: var(--text-medium);">💵 Precio Compra:</dt>
                        <dd class="col-sm-6"><strong>$@Model.PrecioCompra.ToString("N2")</strong></dd>

                        <dt class="col-sm-6" style="color: var(--text-medium);">💲 Precio Venta:</dt>
                        <dd class="col-sm-6"><strong class="text-success">$@Model.PrecioVenta.ToString("N2")</strong></dd>

                        <dt class="col-sm-6" style="color: var(--text-medium);">📊 Stock:</dt>
                        <dd class="col-sm-6">
                            <span class="@(Model.Stock <= Model.StockMinimo ? "badge bg-danger" : "badge bg-success")" style="font-size: 1rem;">
                                @Model.Stock unidades
                            </span>
                        </dd>

                        <dt class="col-sm-6" style="color: var(--text-medium);">⚠️ Stock Mínimo:</dt>
                        <dd class="col-sm-6">@Model.StockMinimo unidades</dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <a asp-action="Index" class="btn btn-secondary">Volver a la Lista</a>
        @if (Context.Session.GetString("UsuarioRol") == "cliente" && Model.Stock > 0)
        {
            <a asp-action="Comprar" asp-route-id="@Model.IdProducto" class="btn btn-success btn-lg">
                🛒 Comprar Ahora
            </a>
        }
        @if (Context.Session.GetString("UsuarioRol") == "admin")
        {
            <a asp-action="Edit" asp-route-id="@Model.IdProducto" class="btn btn-warning">Editar</a>
        }
    </div>
</div>
