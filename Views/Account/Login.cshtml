@{
    ViewData["Title"] = "Iniciar Sesión";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="login-container">
    <div class="login-card">
        <div class="login-header">
            <h3>🔧 Tienda de Repuestos</h3>
            <p class="mb-0 mt-2" style="opacity: 0.9;">Sistema de Gestión</p>
        </div>
        <div class="login-body">
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger" role="alert">
                    <strong>⚠️ Error:</strong> @ViewBag.Error
                </div>
            }

            <form asp-action="Login" method="post">
                <div class="mb-3">
                    <label class="form-label">👤 Tipo de Usuario</label>
                    <div class="btn-group w-100" role="group">
                        <input type="radio" class="btn-check" name="tipoUsuario" id="tipoUsuario" value="usuario" checked>
                        <label class="btn btn-outline-primary" for="tipoUsuario">👨‍💼 Empleado</label>
                        
                        <input type="radio" class="btn-check" name="tipoUsuario" id="tipoCliente" value="cliente">
                        <label class="btn btn-outline-primary" for="tipoCliente">🛒 Cliente</label>
                    </div>
                </div>
                <div class="mb-4">
                    <label for="correo" class="form-label">📧 Correo Electrónico</label>
                    <input type="email" class="form-control" id="correo" name="correo" required autofocus placeholder="tu@correo.com">
                </div>
                <div class="mb-4">
                    <label for="contraseña" class="form-label">🔒 Contraseña</label>
                    <input type="password" class="form-control" id="contraseña" name="contraseña" required placeholder="Ingresa tu contraseña">
                </div>
                <div class="d-grid mb-3">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <strong>Iniciar Sesión</strong>
                    </button>
                </div>
            </form>
            
            <div class="text-center mb-3">
                <p class="mb-0" style="color: var(--text-medium);">¿Eres cliente nuevo?</p>
                <a asp-controller="Cliente" asp-action="Registro" class="btn btn-link" style="text-decoration: none; color: var(--primary-color);">
                    📝 Regístrate aquí
                </a>
            </div>

            <hr class="my-4">
            <div class="text-center">
                <p class="mb-2" style="color: var(--text-medium); font-weight: 600;">👥 Usuarios de Prueba</p>
                <div class="small" style="color: var(--text-light);">
                    <div class="mb-1">
                        <strong>👨‍💼 Admin:</strong> rafael.reyes@tienda.com / Rafael123!
                    </div>
                    <div class="mb-1">
                        <strong>👤 Vendedor:</strong> david.cruz@tienda.com / David123!
                    </div>
                    <div class="mb-1">
                        <strong>🛒 Cliente 1:</strong> marco.benitez@tienda.com / Marco123!
                    </div>
                    <div>
                        <strong>🛒 Cliente 2:</strong> carlos.aranibar@tienda.com / Carlos123!
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
