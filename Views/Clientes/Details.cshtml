@model Tienda_Repuestos_Demo.Models.Cliente

@{
    ViewData["Title"] = "Detalles del Cliente";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1" style="color: var(--text-dark); font-weight: 700;">👤 Detalles del Cliente</h2>
            <p class="text-muted mb-0">Información completa del cliente</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">👤 Información Personal</h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4" style="color: var(--text-medium);">👤 Nombre:</dt>
                        <dd class="col-sm-8"><strong>@Model.Nombre</strong></dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">📧 Correo:</dt>
                        <dd class="col-sm-8">@Model.Correo</dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">📞 Teléfono:</dt>
                        <dd class="col-sm-8">@(Model.Telefono ?? "N/A")</dd>

                        <dt class="col-sm-4" style="color: var(--text-medium);">📍 Dirección:</dt>
                        <dd class="col-sm-8">@(Model.Direccion ?? "N/A")</dd>
                    </dl>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">📊 Información Comercial</h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-5" style="color: var(--text-medium);">✅ Verificado:</dt>
                        <dd class="col-sm-7">
                            @if (Model.Verificado)
                            {
                                <span class="badge bg-success">✓ Sí</span>
                            }
                            else
                            {
                                <span class="badge bg-warning">✗ No</span>
                            }
                        </dd>

                        <dt class="col-sm-5" style="color: var(--text-medium);">📅 Fecha Registro:</dt>
                        <dd class="col-sm-7">@Model.FechaRegistro.ToString("dd/MM/yyyy HH:mm")</dd>

                        <dt class="col-sm-5" style="color: var(--text-medium);">💰 Total Ventas:</dt>
                        <dd class="col-sm-7">
                            <span class="badge bg-primary" style="font-size: 1rem;">
                                @Model.Ventas.Count ventas
                            </span>
                        </dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <a asp-action="Index" class="btn btn-secondary">Volver a la Lista</a>
        @if (Context.Session.GetString("UsuarioRol") == "admin")
        {
            <a asp-action="Edit" asp-route-id="@Model.IdCliente" class="btn btn-warning">Editar</a>
        }
    </div>
</div>
